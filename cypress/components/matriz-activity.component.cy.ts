import { MatrizActivityComponent } from '../../src/app/components/matriz-activity/matriz-activity.component'

describe('MatrizActivityComponent', () => {
  it('should mount', () => {
    cy.mount(MatrizActivityComponent)
  })
})